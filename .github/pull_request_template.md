## What this changes

<!-- One or two sentences. If it adds or corrects a law, name the article. -->

## Checklist

<!-- Delete the section that does not apply. -->

**Codex changes**

- [ ] I edited `data/laws.yaml` only — not `README.md`, `translations/ja.md` or `website/src/data/laws.ts`
- [ ] `npm run validate` passes
- [ ] `npm run generate` was run and its output is committed
- [ ] The source is the primary one — the paper, book, letter, RFC or talk itself — and `npm run check:links` resolves it
- [ ] Both `en` and `ja` are filled in, and they say the same thing
- [ ] `see_also` is reciprocated in both directions

**Site or tooling changes**

- [ ] `npm run lint`, `npm run type-check`, `npm run format:check` and `npm run build` pass in `website/`

## Anything a reviewer should push back on

<!-- Where you were unsure. A claim you could not fully verify, a translation you are not confident in, a design call worth arguing about. -->
